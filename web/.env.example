# Optional. Set your production domain to generate correct canonical URLs, sitemap, and social metadata.
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# Optional. If omitted, local development falls back to the bundled Python extractor.
BACKEND_API_URL=https://your-api-id.execute-api.ap-south-1.amazonaws.com/prod/extract
